@model IEnumerable<Model.DB_Model.Prioridad>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre_Prioridad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha_Insercion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha_Modificacion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre_Prioridad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha_Insercion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha_Modificacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
